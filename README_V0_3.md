# HERMES // ORCA v0.3 pairing patch

Adds a Termux-protected native pairing receiver and installs `hermes-orca-pair`.

After v0.3 is installed, run `hermes-orca-pair` to configure/verify Hermes, save the key inside the app, launch the app, and trigger health refresh.
